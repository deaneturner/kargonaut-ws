import { InlineGridDirective } from './inline-grid.directive';

describe('InlineGridDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineGridDirective();
    expect(directive).toBeTruthy();
  });
});
