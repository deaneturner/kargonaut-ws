import { CompositeLineDirective } from './composite-line.directive';

describe('CompositeLineDirective', () => {
  it('should create an instance', () => {
    const directive = new CompositeLineDirective();
    expect(directive).toBeTruthy();
  });
});
